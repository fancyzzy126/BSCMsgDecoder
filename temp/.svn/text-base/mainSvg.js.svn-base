//author: MJ

var svgdoc, svgroot, ttrelem, tttelem, tt;
function getSVGDoc(load_evt)
{
	svgdoc = load_evt.target.ownerDocument;
	svgroot = svgdoc.documentElement;
	ttrelem = svgdoc.getElementById("ttr")
	tttelem = svgdoc.getElementById("ttt")	
}

function ShowTooltip(evt, txt)
{
	var posx, posy, curtrans, ctx, cty;
	
	posx = evt.clientX;
	posy = evt.clientY;
	curtrans = svgroot.currentTranslate;
	ctx = curtrans.x;
	//	
}

function showFmm(content)
{
	window.parent.showFmm(content)
}

function showRaw(content)
{
	window.parent.showRaw(content)
}

function clearMsg()
{
	window.parent.clearMsg()
}

function showDetailedMsg(msg_content)
{
	window.parent.showDetailedMsg(msg_content)
}

